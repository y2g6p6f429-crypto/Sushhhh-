Not used by default — this project loads Playfair Display, Cormorant Garamond,
Poppins, Inter, and Parisienne from Google Fonts (see the <link> tags in
index.html's <head>). If you'd rather self-host fonts, drop the font files
here and update the @font-face rules / <link> tags accordingly.
