Not needed anymore — the "Music" section and floating player now use a real,
official Spotify embed (see index.html) instead of a self-hosted mp3 file.

This folder is kept in case you'd rather switch back to a self-hosted audio
file later; see the "Music" section of the root README.md for how.
